generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./mydb.db"
}

model Market {
  id         Int    @id @default(autoincrement())
  name       String @unique
  indexPrice Float

  @@index([name])
}
